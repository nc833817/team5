@model IEnumerable<team5_centric.Models.values>

@{
    ViewBag.Title = "Index";
}
<head>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2
        }

        tr:hover {
            background-color: #dbd9d5;
        }
    </style>
</head>

<h2>Core Values</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.valueName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.valueDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.valueIcon)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.valueWeight)
        </th>*@
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.valueName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.valueDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.valueIcon)
            </td>

            @*<td>
                @Html.DisplayFor(modelItem => item.valueWeight)
            </td>*@
            @*<td>
                <i class="glyphicon glyphicon-edit"></i>
                @Html.ActionLink("Edit", "Edit", new { id = item.valueId }) |
                <i class="glyphicon glyphicon-list"></i>
                @Html.ActionLink("Details", "Details", new { id = item.valueId }) |
                <i class="glyphicon glyphicon-trash"></i>
                @Html.ActionLink("Delete", "Delete", new { id = item.valueId })
            </td>*@
        </tr>
    }

</table>